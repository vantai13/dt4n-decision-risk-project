# emulation/ — Mininet validation (CHƯA LÀM)

Chỉ bắt đầu khi RQ1/RQ2 đã có kết quả trong simulation.
Mục tiêu: kiểm tra xu hướng / thứ tự các phương pháp có giữ nguyên trên Linux networking stack
không — KHÔNG cần khớp con số tuyệt đối.

Khi cần, copy từ repo cũ (đã archive) các phần testbed: topology Mininet, controller, bridge đo
telemetry. Lưu ý: KHÔNG đặt tên thư mục là `mininet/` (sẽ che package Mininet thật — lỗi đã gặp).
